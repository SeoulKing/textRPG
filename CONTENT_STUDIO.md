# 작가용 콘텐츠 스튜디오

홈의 **콘텐츠 스튜디오** 또는 `/content-editor`에서 엽니다. 기존 관리자 비밀번호 설정을 그대로 사용합니다.

## 기존 이야기 찾기

이벤트 메뉴의 첫 화면은 **이야기 모아보기**입니다. 기존 프롤로그와 지역 원고를 **주요 이야기 / 지역 소개 / 탐색·수집 / 제작·요리 / 사건·행동 결과**로 분류합니다. 같은 제목의 상황별 원고는 한 카드에서 펼쳐 볼 수 있습니다.

**원고·선택지 검색**에서 장면 제목, 본문, 인물 이름, 아이템 이름을 검색하고 지역·등장인물·작성 상태를 함께 필터링할 수 있습니다. 카드를 열면 기존 원고의 편집 위치로 이동합니다. **이야기 목록** 버튼으로 돌아옵니다.

기존 장면 편집에는 **이 장면이 나오는 흐름**이 표시됩니다. 지역 행동, 재고 수량에 따른 장면, 무작위 결과의 출발점을 확인하고 이동할 수 있습니다. 흐름도에서도 지역 행동과 결과 장면의 연결을 보여 줍니다. 이 분류는 화면에서만 적용되며 기존 원고의 ID·조건·실행 순서를 바꾸지 않습니다.

## 지역 선택지 바로 편집하기

**지역 → 강**을 열면 맨 위의 **이 지역에서 할 수 있는 선택**에 **낚시하기**가 표시됩니다. 카드를 누르면 같은 화면에서 문구, 실행 조건, 확정 보상, 확률 결과와 이어지는 장면을 확인하고 편집할 수 있습니다. 모든 지역에 같은 구성이 적용됩니다.

기존 장면에서는 **이 지역의 공통 행동 편집**을 펼쳐 지역 선택지를 편집합니다. **이야기 모아보기**에서 `낚시하기`를 검색하거나 **지역 선택지** 분류를 선택해도 바로 찾을 수 있습니다. 확률 결과 안의 아이템과 별도로 추가하는 확정 보상은 구분해서 표시합니다.

지역 선택지를 열면 편집 제목 옆의 **선택지 삭제**로 해당 지역에서 제거할 수 있습니다. 일반 장면의 선택지는 카드 오른쪽 위의 **선택지 삭제**를 사용합니다. 삭제 전 영향 범위를 확인하며, 다른 지역·장면의 사용 위치와 연결된 결과 장면 원고는 유지합니다. 삭제한 내용은 **실행 취소**로 복원할 수 있습니다.

## 성공률과 선택 결과 작성

**지역 → 강 → 낚시하기**의 **성공·실패와 확률 결과**에서 기존 성공률과 결과를 바로 편집합니다. 낚시 기본 성공률은 50%이며, 변경하면 성공 결과 사이의 비율을 유지합니다. 예를 들어 60%로 바꾸면 실패 40%, 물고기 1마리 45%, 2마리 15%가 됩니다. 레벨별 실제 성공률도 함께 표시합니다. 기존 게임 규칙에 따라 기본 0%와 100%는 숙련도에 관계없이 해당 결과로 고정됩니다.

- **선택지 추가**로 새로운 지역 행동을 만들고 문구, 행동 종류, 숙련도, 소요 시간, 하루 실행 횟수와 조건을 설정합니다.
- **확률 결과 만들기**, **성공 결과 추가**, **실패 결과 추가**로 분기를 구성합니다. 각 결과의 이름·종류·확률을 직접 바꿀 수 있습니다.
- 결과 카드에서 안내 문장, 체력·정신력·기력·돈 변화, 지급·소모 아이템을 편집합니다. 결과에 넣은 보상은 그 결과가 뽑혔을 때만 지급하며, 공통 보상은 선택지가 정상 실행되면 지급합니다.
- **이 결과의 장면 원고**를 펼치면 기존 결과 장면의 제목과 본문을 같은 화면에서 수정합니다. **새 결과 장면 만들기**로 원고를 새로 만들 수 있고, **전체 장면 편집**에서 대사와 다음 선택지를 추가합니다.
- 결과별 **다음 결과 장면**에 다른 지역의 장면을 고르면 이동 후 시작하도록 연결됩니다. 기존 무작위 장면 묶음도 유지하거나 교체할 수 있습니다.
- **이야기 상태·이동 등 추가 결과**에서는 플래그, 퀘스트, 도구 내구도, 보관함 효과를 설정합니다. **조건 미충족 시 결과**는 확률 실패와 별개입니다.

확률·결과 편집은 기존 게임 효과와 같은 데이터를 사용합니다. 공개하면 새 게임에 적용되며 진행 중인 게임의 고정 콘텐츠 버전은 유지됩니다.
## 이야기 작성

### `/아이템이름`으로 문장에 아이템 연결

- 원고·대사, 장면 제목, 이벤트 진입 문구·안내, 일반·지역 선택지와 결과 안내에서 `/천` 또는 `/천조`를 입력하면 `천 조각`을 찾을 수 있습니다. 방향키로 이동하고 **Enter / Tab / 클릭**으로 확정하며 **Esc**로 닫습니다. 같은 이름은 종류와 ID를 보고 구분합니다.
- 확정한 아이템은 문장 안의 녹색 태그로 보입니다. 예를 들어 `천 조각` 태그 뒤에 `을 건넨다`를 쓰면 게임과 미리보기에는 `천 조각을 건넨다`로 표시합니다. 이름이 아니라 `{{item:clothScrap}}`처럼 ID로 저장하므로 아이템을 개명해도 같은 아이템을 참조합니다.
- 이름 연결만 수행합니다. 보유 조건, 지급·소모 효과와 수량은 기존 편집기로 별도 설정하세요. 아이템 이름 자체나 ID·경로·JSON·검색 입력란에서는 이 기능을 사용하지 않습니다.
- 태그는 Backspace/Delete로 통째로 지우며 **실행 취소 / 다시 실행**으로 복원할 수 있습니다. 스튜디오 태그 편집기 사이의 복사·붙여넣기는 ID를 보존하고, 외부에는 일반 아이템 이름으로 복사합니다. 기존 변수 코드와 조사 선택도 지원합니다.
- 찾지 못했거나 확정하지 않은 슬래시 문구는 그대로 보존합니다. 삭제된 아이템 참조는 붉은 오류 태그로 남기며 다른 아이템으로 자동 연결하지 않습니다. 한글 조합 중에는 태그 확정이나 편집 영역 재생성을 하지 않습니다.

### 새 이야기 만들기

1. **지역 → ＋**에서 이름과 소개, 기존 지역과 이어지는 왕복 길, 첫 장면을 작성합니다. 생성 후 지도에서 빈 칸을 선택해 위치를 조정하고 발견 조건을 설정합니다. 현실과 마법도시는 별개의 지도 좌표를 사용합니다.
2. **캐릭터 → ＋**에서 인물 설정과 머무는 지역을 작성합니다. **대화 이벤트 만들기**를 누르면 화자가 연결된 첫 장면이 만들어집니다.
3. **이벤트**에서 설명문 또는 인물 대사를 작성합니다. **선택지 추가**로 플레이어가 할 말이나 행동을 만듭니다.
4. 선택지의 **아이템 추가**에서 보상을 검색하고 수량을 지정합니다. 같은 아이템을 다시 선택하면 수량이 합쳐집니다. **새 아이템 만들기**는 원고를 유지한 채 아이템을 생성하고 보상에 연결합니다. 새 도구의 기본 내구도는 10이며 상세 설정은 아이템 메뉴에서 바꿀 수 있습니다.
5. **소모하는 아이템**은 보상과 별도입니다. 새 이벤트에서 필요한 소모 아이템이 부족하면 실행할 수 없습니다. 보상만 설정한 선택지에는 소모 조건이 생기지 않습니다.
6. **다음 장면**, **새 장면을 만들고 연결**, **다음 이벤트**로 흐름을 연결합니다. 다른 지역의 이벤트를 고르면 이동 후 시작합니다. 종료 장면은 도착했을 때 완료를 기록합니다. 선택지가 없는 종료 장면에는 기본 종료 버튼이 제공됩니다. **이 선택으로 현재 이벤트 마치기**는 선택 후 기본 지역 장면으로 돌아갑니다.
7. 후속 이벤트의 **이전 이야기와 연결**과 **발생 조건**을 설정합니다. 예를 들어 앞 이벤트를 완료하고 2일째부터 급식소에서 시작하도록 만들 수 있습니다. 조건은 모두 충족해야 합니다.

새 이벤트는 기본 한 번만 발생합니다. 반복 이벤트 안의 보상을 제한하려면 선택지의 **한 번만 선택 가능**을 사용하세요.

## 흐름도와 시험 플레이

- 흐름도는 **흐름도 보기**로 펼치는 보조 보기입니다. PC 오른쪽에는 게임 미리보기가 기본 표시됩니다.
- **이벤트 내부**는 장면과 분기, **전체 이야기**는 이벤트 연결을 보여줍니다. 실선은 즉시 연결, 점선은 이전 이야기 조건입니다.
- 카드 윗부분을 끌어 배치하고 제목을 눌러 원고를 엽니다. 자동 정렬과 확대·축소를 지원하며 카드 위치도 저장됩니다.
- **연결 검사**의 오류를 누르면 해당 콘텐츠로 이동합니다. 의도적인 반복 분기는 허용하며 연결되지 않은 장면은 경고합니다.
- 오른쪽 **시험 조건**을 펼쳐 지역·일차·소지품·진행 상태를 지정합니다. 실제 게임의 보상·조건 엔진을 사용하지만 일반 게임 세이브와는 분리됩니다. 시험 세션은 서버 재시작 또는 한 시간 미사용 시 만료됩니다.

## 오른쪽 게임 미리보기

- PC에서는 목록·원고·약 400px 폭의 게임 미리보기를 함께 표시합니다. 미리보기는 원고와 독립적으로 스크롤하며, 좁은 화면에서는 **원고 편집 / 게임 미리보기**로 전환합니다.
- 장면을 열면 같은 장면에서 시험을 준비합니다. 선택지를 누르면 실제 조건·보상·확률 엔진으로 진행하고, 실제로 도착한 장면을 중앙 편집기에 엽니다. 현재 지역·일차·장면, 선택 경로와 실제 결과 안내를 함께 표시합니다.
- 문단·선택지를 입력하면 원고는 즉시 반영되고 편집 중인 부분을 강조합니다. 별도 **편집** 버튼은 선택지를 실행하지 않고 원본으로 이동합니다. 공통 선택지는 현재 장면의 사용 위치로, 지역 행동은 지역 선택지 편집기로 연결됩니다.
- 규칙 변경은 짧은 지연 후 반영하며, 최신 변경 반영이 끝나야 다음 선택을 실행할 수 있습니다. 한글 조합 중에는 서버 갱신을 보류합니다. 이미 받은 보상과 지난 선택은 소급 변경하지 않습니다.
- 다른 장면을 직접 열면 시험 상태를 유지하고 경로에 **편집기에서 직접 이동**을 기록합니다. 확률 분기는 실제로 뽑힌 도착 장면만 기록합니다.
- **직전 단계**는 시험 상태만 복원하며 최신 원고를 유지합니다. **현재 장면부터 다시 시작**은 시험을 새로 시작합니다. 편집기의 실행 취소와 시험 이력은 별개입니다.
- 미완성 원고·잘못된 연결·맞지 않는 조건이 있으면 **원고 표시 중 · 시험 진행 불가**와 원인을 표시합니다. 기존 시험 상태를 보존하며 오류 수정 또는 명시적인 재시작 전까지 진행을 잠급니다. 편집 중인 숨겨진 선택지는 별도 안내 영역에서 확인합니다.
- 초안 갱신은 세션 전용 메모리의 콘텐츠만 교체합니다. 미리보기 때문에 영구 콘텐츠 버전이 쌓이지 않으며, 세션 종료·만료 시 메모리를 해제합니다. 공개본과 실제 게임 세이브를 읽거나 변경하지 않습니다.

## 저장과 공개

### 원고 중심 편집과 실행 취소

- 이벤트를 열면 원고를 하나의 문서 면에서 작성합니다. 제목 옆의 **이야기 설정**을 누르면 헤더 아래에 제목·발생 지역·조건을 펼칩니다. 종료 여부는 **장면 실행 설정**에 있습니다. 왼쪽 장면 목록으로 이동하고, **흐름도 보기**는 필요할 때 펼칩니다.
- 문단의 **문단 도구**에서 복제·위로·아래로·삭제를 사용합니다. 키보드로 도구를 열고 Tab으로 이동할 수 있으며 Esc로 닫으면 도구 버튼으로 돌아갑니다. 메뉴를 열고 닫는 동작은 원고나 실행 취소 이력을 변경하지 않습니다. 아이템 연결 도움말은 원고 영역에 한 번만 표시합니다.
- 원고 아래 선택지 카드에서 문구, 조건·보상 요약과 목적지를 함께 확인합니다. **다음 진행**에서 기존 장면, 새 장면, 다른 이벤트, 종료를 선택합니다. 확률·실패 결과 안의 이동 규칙은 자동 변환하지 않으며 **상세 조건·보상·확률 설정**에서 편집합니다.
- 선택지 카드 안의 조건·보상·행동 원고는 제목과 구분선으로 나눕니다. 빈 조건·보상·소모는 요약 행의 **추가** 버튼으로 설정하며, **선택지 삭제**는 카드에 계속 표시합니다. 종료·다른 이벤트 연결을 사용할 때 행동 원고는 짧은 상태 안내로 표시하고, 연결 오류와 누락된 원고 경고는 유지합니다.
- **공통 선택지 · 적용되는 장면 N개**를 펼치면 수정이 반영될 장면을 확인할 수 있습니다. **복제**한 선택지는 새 ID를 가져 원본과 독립적으로 편집됩니다. 문단과 선택지는 복제·위로·아래로 이동을 지원합니다.
- **실행 취소 / 다시 실행**, `Ctrl+Z`, `Ctrl+Shift+Z` 또는 `Ctrl+Y`는 문서 전체와 공유 참조를 함께 복원합니다. 현재 편집 세션의 최근 이력만 보관하며(최대 60단계, 메모리 한도에 따라 감소) 새로고침하면 초기화됩니다. 자동 저장된 변경도 현재 세션에서는 취소할 수 있습니다.
- **이야기 목록**으로 돌아가면 검색어·분류·필터·펼친 장면 목록·스크롤 위치를 복원합니다. **장면 맥락**에서 들어오는 연결과 이어지는 연결로 이동할 수 있습니다.
- 시험 조작은 오른쪽 게임 미리보기에 모았습니다. **현재 장면부터 다시 시작**은 마지막 시험 조건을 재사용해 해당 장면에서 다시 시작합니다. 장면 표시 조건이 맞지 않으면 하나의 **시험 조건 설정** 접이식 영역에서 날짜·아이템·진행 상태를 바꾸세요. 좁은 화면에서는 **원고 편집 / 게임 미리보기** 탭으로 이동합니다. 이야기의 진입 과정만 건너뛰며 장면·선택 조건과 실제 보상 엔진은 그대로 사용합니다.
- 시험 중 **직전 단계**는 선택 또는 상황 변경 전의 소지품·능력치·플래그·진행 기록을 함께 복원합니다(최대 100단계). 마지막 시작 조건은 브라우저 편집 세션 동안 이벤트별로 재사용합니다.
- **게임에 공개**를 누르면 현재 공개본 대비 변경된 이야기·선택지·아이템 등의 목록과 이전/이후 내용을 먼저 확인합니다. **변경 확인 후 공개**를 눌러야 실제 공개됩니다.

### 저장 동작

- 원고 수정 후 자동으로 초안을 저장합니다. **초안 저장**으로 직접 저장할 수도 있습니다. 미완성 문장과 연결도 초안에 보관됩니다.
- **게임에 공개**는 검증 후 새 게임이 사용할 콘텐츠 버전을 만듭니다. 이미 진행 중인 게임과 수동 세이브는 원래 버전을 유지합니다.
- 두 탭에서 같은 초안을 편집하면 먼저 저장한 탭의 변경을 보호합니다. 충돌한 탭에서는 원고를 내보낸 뒤 페이지를 새로고침해 최신 초안을 확인하세요.
- NPC 원고는 작성한 그대로 표시됩니다. 기존 게임의 별도 NPC 생성형 대화 기능은 유지됩니다.

## 저장 형식과 호환성

스튜디오 문서는 `version: 2`입니다. 버전 1은 읽을 때 변환합니다. 기존 ID, 조건·효과, 아이템·레시피, 지역 행동과 장면은 보존하며 이름을 바꿔도 ID는 바뀌지 않습니다. 아이템 보상은 별도 지급 목록을 만들지 않고 기존 `add_item` 효과로 저장합니다.

로컬 환경의 새 초안·공개 포인터는 `RUNTIME_DIR/content-studio-store.json`에 함께 원자적으로 저장됩니다. 최초 실행 시 기존 `content/content-studio.json`과 `RUNTIME_DIR/content-studio-draft.json`을 가져옵니다. 기본 `RUNTIME_DIR`는 `.runtime`입니다. 운영 환경은 기존 `content_studio_documents` PostgreSQL 테이블을 사용합니다.

불변 실행 버전은 로컬의 `RUNTIME_DIR/content-versions/` 또는 PostgreSQL의 `content_versions` 테이블에 보존합니다. 세이브의 `contentVersionId`가 이를 참조하므로 **게임 세이브와 콘텐츠 버전 저장소를 함께 백업해야 합니다**. 버전 없는 기존 세이브는 최초 전환 시점의 공개 콘텐츠에 연결됩니다. 필요한 버전이 없으면 오류로 중단하며 최신 콘텐츠로 몰래 대체하지 않습니다.

API: 기존 `GET/PUT /api/content-studio`, `POST /api/content-studio/publish`를 유지합니다. 저장·공개에는 마지막 `draftUpdatedAt`을 `If-Match` 헤더로 보내며, 초안이 없으면 `none`을 보냅니다. 충돌은 409, 헤더 누락은 428입니다. `POST /api/content-studio/validate`는 위치 정보가 있는 검사 결과를, `POST /api/content-studio/preview`와 `POST /api/content-studio/preview/:id`는 시험 플레이 시작·진행을 제공합니다. 모든 API에 기존 관리자 인증이 적용됩니다.

검증 명령: `npm run test:studio` (빌드 및 콘텐츠·보상·세이브 호환 테스트).

읽기 전용 `GET /api/content-studio/published`는 공개 전 비교용 공개 문서와 시각을 반환합니다. 시험 시작의 `setup.sceneId`는 같은 `storyId`에 속한 장면만 허용하며 실제 게임 API에는 추가되지 않습니다. 시험 진행에서 `{ "undo": true }`를 보내면 직전 시험 상태로 돌아갑니다. 기존 문서 버전과 저장·공개 요청 형식은 유지됩니다.

시험 진행 요청에는 최신 초안을 `document`로 함께 보낼 수 있습니다. `{ document }`는 상태를 유지한 초안 갱신, `{ document, jump: { storyId, sceneId } }`는 편집기의 직접 이동입니다. `action`·`setup`·`undo`·`jump` 중 하나만 함께 보낼 수 있습니다. 갱신 실패 시 콘텐츠와 게임 상태를 모두 이전 상태로 보존합니다. 응답에는 편집 대상 `editorTarget`, 실제 결과 `systemNoteEntries`, 선택 전후 장면이 포함된 `trace`를 제공합니다. `DELETE /api/content-studio/preview/:id`는 해당 시험 세션과 임시 콘텐츠를 해제합니다.

## 장소 선택지의 행동 묘사와 결과

- 지역을 열면 선택지 카드에서 기본 보상 또는 가능한 확률 결과를 바로 확인할 수 있습니다. 숲의 **벌목하기**에 있던 세 장면은 서로 다른 보상 세 개가 아니라 같은 벌목을 다르게 묘사하는 원고 세 개입니다.
- 확정형은 **장소 기본 화면 → 행동 묘사 → 공통 결과 → 기본 화면 복귀**로 표시합니다. **행동 묘사** 블록에서 여러 원고를 추가·편집하고, 결과는 한 번만 설정합니다. 원고를 바꾸어도 아이템이 중복 지급되지 않습니다.
- 확률형은 가능한 결과를 모두 카드로 표시합니다. 각 카드에 기본 확률, 얻는 아이템, 해당 결과의 행동 묘사 개수가 나타납니다. 카드를 누르면 그 결과의 확률·아이템·소모·원고를 편집할 수 있습니다. 성공률에 영향을 주는 숙련도는 레벨별 확률로 함께 표시합니다.
- **원고 추가**는 빈 대체 원고를 만들고, **기존 장면 가져오기**는 이름으로 원고를 찾아 연결합니다. **묶음에서 빼기**는 해당 행동과의 연결만 해제합니다. 기존 장면 본문은 공유되므로 같은 원고를 사용하는 다른 곳에도 본문 수정이 반영됩니다.
- **같은 원고가 연속으로 나오지 않게**를 켜면 조건을 충족한 대체 원고가 두 개 이상일 때 직전에 사용한 원고를 제외합니다. 기본 화면으로 돌아갔다가 다시 행동해도 직전 원고를 기억합니다.
- 기존 흐름은 그대로 불러옵니다. **묘사 → 결과 → 기본 화면 복귀 적용**을 누르면 각 결과 화면에 **숲으로 돌아가기**와 같은 버튼을 붙입니다. 복귀할 때 보상과 소요 시간은 다시 적용하지 않습니다. 자체 후속 선택지가 있는 장면은 이 설정으로 덮어쓰지 않습니다.
- 원고 묶음은 기존 `set_random_scene` 효과의 선택적 `sceneIds`, `avoidRepeat`, `returnToLocation`로 저장합니다. 기존 태그 기반 선택은 그대로 지원하며, 복귀 장면은 실행 콘텐츠를 만들 때 생성합니다. 시험 응답의 `sourceSceneId`는 생성된 실행 장면과 편집할 원본 원고를 연결합니다.

### 남은 내용물에 따른 상황 연결

‘계산대로 간다’, ‘진열대로 간다’처럼 보관 장소를 살펴보는 선택지에는 **선택 후 이어지는 상황**을 펼쳐 표시합니다. 각 카드에는 남은 돈·아이템의 조건, 도착 장면, 그곳에서 할 수 있는 선택지가 나옵니다. 상황을 누르면 아래에 해당 원고 미리보기와 편집 버튼을 표시합니다. 조건은 실제 저장된 기준을 그대로 표시하며 확률 분기와 구분합니다.

**이 상황의 원고 열기** 또는 후속 선택지를 누르면 해당 원고·선택지 편집기로 이동합니다. **이전 선택의 연결로 돌아가기** 버튼으로 원래 선택지로 돌아올 수 있습니다. 이 연결 표시는 기존 조건과 게임 진행을 변경하지 않습니다.

### 보관 장소에서 가져오는 아이템과 돈

**얻는 아이템**에는 고정 지급뿐 아니라 진열대·보관함에 남은 아이템을 가져오는 결과도 표시됩니다. 예를 들어 캔 음식 선택지는 **캔 음식 / 편의점 폐허 · 진열대 / 남은 수량 전부 / 처음 3개**로 표시합니다. 같은 효과를 보상 목록과 게임 실행에서 함께 사용하므로 표시를 위해 별도의 지급 효과를 추가하지 않습니다.

**남은 수량 전부 / 최대 수량 지정**으로 가져오는 방식을 수정합니다. 지정한 수량보다 적게 남았다면 남은 만큼 가져오고, 비어 있으면 지급하지 않습니다. 이미 ‘전부 가져오기’로 연결된 아이템을 다시 추가해도 중복 지급을 만들지 않습니다. 계산대 등에서 가져오는 돈은 **얻는 돈**에 같은 방식으로 표시됩니다.

**보관 장소의 처음 수량 설정**에서는 새 게임에 놓일 수량을 수정할 수 있습니다. 같은 보관 장소를 쓰는 선택지들이 이 수량을 공유합니다. 진행 중인 시험에서 이미 가져간 수량은 유지되므로 처음 상태는 시험을 다시 시작해 확인합니다. 공개 후에도 기존 게임은 저장된 콘텐츠 버전의 초기 수량을 사용합니다.
