# 픽셀 디오라마 거처 장면 Design QA

## 기준

- 배경: `assets/scenes/cooking/shelter-cooking-diorama.png` (`768 × 224`)
- 캐릭터: `assets/scenes/cooking/survivor-cooking-cycle-v2.png`
- 전경 소품: `assets/scenes/cooking/foreground-supply-crate.png`
- 캐릭터 규격: `96 × 128` 고정 셀 12개(걷기 8, 생활 대기 4)
- 목표 뷰포트: `384 × 824` CSS px
- 장면 상태: `hidden ↔ ready(rest/crafting/cooking)`, 위치 전환 중 `moving`

## 시각 방향

- 배경·캐릭터·소품을 같은 16비트 픽셀 밀도와 따뜻한 흙빛 팔레트로 통일했습니다.
- 천막, 침낭, 화로, 제작대가 모바일에서도 한눈에 읽히도록 실루엣과 명암 덩어리를 단순화했습니다.
- `background → ambient → shadow → actor → foreground` 순서의 다섯 레이어로 구성했습니다.
- 캐릭터가 이동 중 낮은 보급 상자 뒤를 지나면서 하체가 가려져 실제 공간을 걷는 깊이감을 줍니다.
- 화로 광원과 랜턴 광원이 서로 다른 속도로 흔들리고, 불티와 냄비 김이 독립적으로 반복됩니다.

## 동작 결과

- 거처 대기는 `(235, 207)`, 제작대는 `(395, 207)`, 화로는 `(445, 207)`을 사용합니다.
- 위치 전환은 초당 `123px`을 기준으로 최소 0.7초 동안 선형 이동하며, 왼쪽으로 걸을 때 스프라이트 방향도 전환합니다.
- 걷기는 14fps, 생활 대기는 4fps이며 이동·도착 전 과정에서 배율 `1.2`와 발 위치 `y=207`을 유지합니다.
- 스프라이트 12개 프레임의 발 기준선은 셀 내부 `y=124`, 실루엣 높이 편차는 1px 이하입니다.
- `prefers-reduced-motion`에서는 이동과 환경 반복 동작을 멈추고 도착 위치의 대기 첫 프레임을 표시합니다.
- Phaser 모듈 또는 장면 에셋 로딩이 실패하면 캔버스를 숨기고 기존 장소 이미지를 유지합니다.
- 거처 기본·제작·요리 장면 모두 `24 / 7` 비율을 사용해 모바일에서 `384 × 112`로 표시됩니다.

## 검증 결과

- 브라우저 ESM 구문 검사 통과
- 서버 TypeScript 타입 검사 통과
- 스프라이트 규격 검사 통과: 12프레임, `96 × 128`, 기준선 124, 실루엣 120–121px
- 새 배경·캐릭터·전경 에셋과 그래픽 모듈 HTTP 200
- `384 × 824` 실제 미리보기:
  - 장면 영역 `384 × 112`
  - 이동·도착 중 발 위치 `y=207`, 배율 `1.2` 유지
  - 거처 대기 → 제작대 → 화로 → 거처 대기 이동 확인
  - 전경 제작대 뒤 하체 가림 확인
  - 화로 광원·불티·냄비 김·생활 대기 루프 동시 재생
  - 콘솔 경고·오류 없음

final result: passed
