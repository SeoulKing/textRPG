# 작업 원칙

- 사용자에게는 항상 존댓말로 답변합니다.
- 정확성을 유지하되, 같은 결론을 주는 도구 호출·검색·검증을 반복하지 않습니다.
- 작업을 시작할 때 `.agents/LESSONS.md`에서 현재 작업과 관련된 항목만 확인합니다.

## 경제적인 실행 루프

1. 요청을 한 문장으로 고정하고, 결과를 입증할 가장 싼 증거를 먼저 정합니다.
2. 변경 범위를 확인한 뒤 필요한 파일만 읽고 수정합니다.
3. 실패하면 `도구/명령 + 대상 + 핵심 오류`를 실패 지문으로 취급합니다.
4. 같은 실패 지문은 전제 조건이 달라지지 않은 한 다시 시도하지 않습니다.
5. 재시도는 설정 변경, 서버 기동, 권한 확보 등 성공 가능성을 바꾸는 구체적인 변화가 있을 때 한 번만 합니다.
6. 충분한 증거가 확보되면 검증을 종료합니다. 단지 안심하기 위한 중복 검증은 하지 않습니다.
7. 작업 종료 시 새롭고 재사용 가능한 교훈만 `.agents/LESSONS.md`에 합칩니다.

## 검증 선택

변경과 직접 연결된 가장 작은 검증부터 선택합니다.

- 문서·지침만 변경: 변경 내용 재확인. 코드 빌드와 브라우저 검증은 생략합니다.
- TypeScript 로직 변경: 관련 테스트를 우선하고, 타입 경계를 바꿨을 때만 `npm.cmd run typecheck`를 추가합니다.
- 콘텐츠 정의 변경: `npm.cmd run content:validate`를 우선합니다.
- 독립 JavaScript 파일 변경: 해당 파일의 구문 검사부터 수행합니다.
- 빌드 설정, 배포 산출물, 컴파일 경계를 변경: 빌드를 수행합니다.
- 화면 배치·스타일·실제 브라우저 상호작용을 변경: 정적 검증으로 확인할 수 없는 핵심 경로만 브라우저에서 한 번 확인합니다.

전체 검증 명령을 관성적으로 연속 실행하지 않습니다. 하나의 검증이 이미 같은 위험을 충분히 다뤘다면 다음 검증은 생략합니다.

## 브라우저와 Playwright

- Playwright는 기본 검증 수단이 아닙니다.
- 이 저장소에 실행 가능한 Playwright 환경이 확인되지 않았거나 같은 실행 경로가 이미 실패했다면 다시 호출하지 않습니다.
- 브라우저 검증이 꼭 필요할 때만 작업당 한 세션을 사용하고, 필요한 DOM·동작·화면 확인을 그 세션에 묶습니다.
- 브라우저 실행이 실패하면 오류 원인과 전제 조건을 기록하고, 정적 검사나 관련 테스트가 질문에 답할 수 있으면 그 방법으로 전환합니다.
- 브라우저에서만 확인 가능한 요구사항인데 환경 문제로 막히면 성공한 것처럼 간주하지 않고, 미검증 항목 하나만 명확히 보고합니다.
- 검증만을 위해 새 도구나 의존성을 설치하지 않습니다. 사용자가 설치 또는 환경 구성을 요청한 경우는 예외입니다.

## 학습 기록 규칙

`.agents/LESSONS.md`는 작업 일지가 아니라 다음 의사결정을 바꾸는 작은 메모리입니다.

- 사용자 선호, 반복 가능한 실패 지문, 확인된 대체 경로만 기록합니다.
- 같은 내용은 새 줄로 늘리지 않고 기존 항목을 갱신합니다.
- 일회성 오류, 긴 실행 로그, 이미 코드나 문서에서 명백한 사실은 기록하지 않습니다.
- 항목에는 다시 시도할 수 있는 조건을 함께 적어 영구 금지가 되지 않게 합니다.
- 항목이 20개를 넘으면 중복되거나 오래된 항목을 합치거나 제거합니다.

